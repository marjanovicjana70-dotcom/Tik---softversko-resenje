<UserControl x:Class="JanaTakmicenje22.Views.Pages.NotesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#E0F4FF" BorderBrush="#E0E6ED" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="25,30,25,10" Orientation="Horizontal">
                    <Image Source="/Assets/logo2.png" Height="30" Margin="0,0,10,0"/>
                    <TextBlock Text="BELEŠKE" FontSize="18" FontWeight="Black" Foreground="#5D8EB0" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Row="1" Click="NewNote_Click" Margin="20,10" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBg" Background="#7CADCC" CornerRadius="12" Padding="15,12">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="+" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,8,0"/>
                                    <TextBlock Text="NOVA BELEŠKA" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBg" Property="Background" Value="#497999"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <ListBox x:Name="NotesList" Grid.Row="2" 
                         ItemsSource="{Binding Notes}"
                         Background="Transparent" BorderThickness="0" 
                         SelectionChanged="NotesList_SelectionChanged" Margin="10,0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="12" Padding="15" Margin="0,4">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#EDF2F7"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#96C0D9"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Bold" Foreground="#497999"/>
                                <TextBlock Text="{Binding UpdatedAt, StringFormat='dd.MM.yyyy'}" FontSize="11" Foreground="#7CADCC" Margin="0,4,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <StackPanel x:Name="EmptyState" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/Assets/logo2.png" Height="80" Opacity="0.15"/>
                <TextBlock Text="Izaberi belešku" FontSize="16" Foreground="#7CADCC" Margin="0,15,0,0" FontWeight="SemiBold"/>
            </StackPanel>

            <Grid x:Name="EditorPanel" Visibility="Collapsed" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="#F0F4F8" BorderThickness="0,0,0,2" Padding="0,0,0,15" Margin="0,0,0,20">
                    <TextBox Text="{Binding EditTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="28" FontWeight="Black" Foreground="#7CADCC"
                             Background="Transparent" BorderThickness="0"/>
                </Border>

                <Border Grid.Row="1" Background="Transparent">
                    <TextBox Text="{Binding EditContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="16" Foreground="#497999" Background="Transparent" 
                             BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                </Border>

                <Grid Grid.Row="2" Margin="0,25,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Click="SaveNote_Click" Cursor="Hand" Margin="0,0,12,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="saveBg" Background="#5D8EB0" CornerRadius="10" Padding="25,12">
                                        <TextBlock Text="SAČUVAJ" Foreground="White" FontWeight="Bold"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Click="DeleteNote_Click" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="White" BorderBrush="#7CADCC" BorderThickness="1" CornerRadius="10" Padding="20,12">
                                        <TextBlock Text="OBRIŠI" Foreground="#497999" FontWeight="Bold"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                    <TextBlock Text="{Binding SaveStatus}" HorizontalAlignment="Right" VerticalAlignment="Center" 
                               Foreground="#3D6B99" FontWeight="Bold"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>